# Claude Code

This project's instructions are tool-independent and live in `AGENTS.md`, which
every agent reads. This file only imports them, so there is one source of truth
rather than a copy per tool.

@AGENTS.md
