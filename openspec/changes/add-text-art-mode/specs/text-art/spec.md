## ADDED Requirements

A new capability. Every requirement in `openspec/specs/text-art/spec.md` is
added by this change:

- The grid is the resolution, and nothing else changes
- The three inks come from the hour's sky
- The tone curve follows the sky it is drawing
- The ridge does not beat against the dot grid
