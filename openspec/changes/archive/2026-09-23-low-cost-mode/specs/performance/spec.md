## ADDED Requirements

A new capability. Every requirement in `openspec/specs/performance/spec.md` is
added by this change:

- The cost is chosen by how the machine is powered
- The cheaper scene is the same scene
- The shader is told the size it is drawing at
